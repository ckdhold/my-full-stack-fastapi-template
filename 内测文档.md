# UMP 内测验收指南（W6–W8）

## 环境

| 组件 | 地址 |
|------|------|
| 前端 | http://localhost:5279 |
| 后端 API | http://localhost:6279 |
| 默认账号 | admin@example.com / changethis |

## 验收清单

### 1. 指标 Explorer（W6）

1. 登录后进入 **指标查询** `/metrics`
2. 选择目标、指标名、时间范围（1h / 6h / 24h / 7d）
3. 切换 **折线图** / **数据表** 标签，确认有数据时图表正常渲染

### 2. 预置仪表盘（W6）

1. 进入 **仪表盘** `/dashboards`
2. 打开「平台总览 / 响应延迟 / 主机资源」任一大盘
3. 各 Panel 应展示对应 metric 折线图

### 3. 事件时间线（W6）

1. 工作台应显示 **最近事件** 卡片
2. 进入 **事件中心** `/events` 查看完整时间线
3. 触发告警、Agent 离线、创建目标后应产生新事件

### 4. 目标详情（W6）

1. 在 **监控目标** 列表点击目标名称
2. 详情页 `/targets/:id` 应展示指标图、告警与事件

### 5. Push Gateway（W7）

1. 创建类型为 `business` 的监控目标
2. **管理 → API Token** 创建 Token 并关联该目标
3. 推送示例：

```bash
curl -X POST http://localhost:6279/api/v1/ingest/push/metrics \
  -H "Content-Type: application/json" \
  -H "X-Api-Token: ump_xxx" \
  -d '{"metrics":[{"metric":"biz.orders.count","value":42}]}'
```

4. 在指标查询中确认数据；配置告警规则后可触发通知

### 6. 数据库探测（W7）

1. 创建 `database` 类型目标，config 示例：`{"host":"db","port":5432}`
2. 等待调度器探测（默认约 60s）
3. 指标 `probe.db.up` / `probe.db.response_time_ms` 应写入

### 7. 告警静默（W8）

1. 进入 **告警静默** `/alerts/silences`
2. 为目标或全部目标创建维护窗口
3. 窗口内阈值 breach 不应产生新 firing 告警

### 8. 持续触发 duration_sec（W8）

1. 创建告警规则并设置 **duration_sec**（如 300 秒）
2. 仅当指标在整个窗口内持续超阈值时才 firing

### 9. 值班表 / 审计（W8）

- **通知中心 → 值班表** `/notifications/oncall` 展示 seed 联系人
- **管理 → 审计日志** `/admin/audit` 记录 Token / 静默等操作

## 已知限制

- 仪表盘 Panel 默认绑定同类型第一个目标，多目标场景需在指标查询中细查
- 数据库探测为 TCP 连通性检查，非 SQL 级健康检查
- `/items` 演示模块已从菜单隐藏

## 问题反馈

内测请记录：操作步骤、期望结果、实际结果、截图/请求 ID，提交至项目 Issue。
