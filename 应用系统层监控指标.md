# 应用系统层监控指标参考

> 从服务进程内部观测运行状况：稳不稳、快不快。不含主机资源、黑盒探测、业务 KPI。  
> 上报：Push Gateway（`POST /api/v1/ingest/push/metrics`）或应用内埋点。

| 列 | 含义 |
|----|------|
| 是否可采集 | 常规技术栈下能否稳定拿到 |
| 有效性 | 对故障发现、容量、SLA 的价值 |
| 采集方式 | 推荐落地手段 |

---

## 指标总表

| 指标 | 是否可采集 | 有效性 | 采集方式 |
|------|------------|--------|----------|
| **请求性能** | 是 | 看流量、慢不慢（QPS、P95/P99），核心 SLA | 网关/中间件埋点、APM、定时 Push；含：请求量、耗时 |
| **服务可用性** | 是 | 看错没错、能不能对外服务；适合 P0/P1 告警 | 状态码与异常汇总、健康检查接口；含：错误率、健康状态 |
| **运行时资源** | 是 | OOM、CPU 飙高、GC 导致卡顿 | 进程监控、JVM/运行时接口、Agent 或 Push；含：CPU、内存、GC（JVM） |
| **资源饱和度** | 视架构 | 快顶满：排队、拿不到连接、消息积压 | 线程池/连接池/MQ 组件指标；Push |
| **依赖调用** | 是 | 区分本服务慢还是下游、级联故障 | 出站 HTTP/gRPC 客户端埋点、调用链；含：下游耗时、失败率 |
| **稳定性扩展** | 视架构 | 限流、熔断、缓存失效、链路排障 | 框架与网关计数、缓存命中、Trace 写日志；二期按需 |

---

## 上报命名（UMP，每类 1～2 个即可）

| 指标类 | 建议上报名 |
|--------|------------|
| 请求性能 | `app.request.count`、`app.request.duration_ms` |
| 服务可用性 | `app.request.error_rate`、`app.health.up` |
| 运行时资源 | `app.process.*`（如 cpu、memory） |
| 资源饱和度 | `app.pool.*`、`app.mq.lag` |
| 依赖调用 | `app.client.duration_ms`、`app.client.error_rate` |

---

## 与其他层

| 层级 | 与应用层区别 |
|------|----------------|
| 基础设施 | 看整机，不看进程内逻辑 |
| 合成探测 | 外部通不通，看不到 5xx 构成与连接池 |
| 业务 KPI | 订单/转化等，不是接口 RED |

**MVP**：先做 **请求性能 + 服务可用性** 四类子项即可（量、耗时、错误率、健康）。

---

*v1.3 | 详见 `监控平台方案.md`*
